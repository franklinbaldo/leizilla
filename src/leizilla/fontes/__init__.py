"""Definições de fontes por ente federativo.

Cada submodule `fontes/{ente}.py` declara:
- FONTES: list[str]  — slugs de fontes disponíveis (ex: ['casacivil', 'assembleia'])
- BASE_URL: str      — URL raiz do portal principal
- FONTE_CANONICA: str — fonte primária para o vigente compilado
"""
